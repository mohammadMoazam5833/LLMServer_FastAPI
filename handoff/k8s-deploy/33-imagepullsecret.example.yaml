# Optional — only if Docker Hub repo is PRIVATE.
# Prefer: kubectl -n team-b create secret docker-registry llm-registry ...
apiVersion: v1
kind: Secret
metadata:
  name: llm-registry
  namespace: team-b
  labels:
    app.kubernetes.io/name: llm-gateway
    app.kubernetes.io/part-of: llm-fastapi
type: kubernetes.dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "https://index.docker.io/v1/": {
          "username": "mohammadmoazami007007",
          "password": "CHANGE_ME_DOCKERHUB_READ_TOKEN"
        }
      }
    }
